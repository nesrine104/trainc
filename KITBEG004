#include <stdio.h>

int main() {
    long long a, b; 
    int da, db; 
    scanf("%lld %d", &a, &da);
    scanf("%lld %d", &b, &db);
    printf("a =%*lld\n", da, a);
    printf("b =%*lld", db, b);

    return 0;
}

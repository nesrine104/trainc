#include <stdio.h>

int main() 
{
    int a, b;
    scanf("%d\n%d", &a, &b);
  	for(int i = a; i <= b; i++) {
        char* m[10] = {"no", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
        if (i<=9)
            printf("%s\n", m[i]);
        else if ((i>9)&(i%2==0))
            printf("even\n");
        else printf("odd\n");
        
    }

    return 0;
}
